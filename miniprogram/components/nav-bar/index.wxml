<view class="nav-bar" style="padding-top: {{statusBarHeight}}px; background: {{bg}};">
  <view class="nav-bar__body" style="height: {{navBodyHeight}}px;">
    <view class="nav-bar__side">
      <view class="nav-bar__back" wx:if="{{showBack}}" hover-class="nav-bar__back--hover" bindtap="handleBack">
        <text class="nav-bar__back-icon">‹</text>
      </view>
    </view>
    <view class="nav-bar__title">{{title}}</view>
    <view class="nav-bar__side"></view>
  </view>
</view>
